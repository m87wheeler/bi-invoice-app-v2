import styled from "styled-components";

const Wrapper = styled.div``;

const ClientWorks = (props) => {
  return (
    <Wrapper>
      <h1>Works</h1>
    </Wrapper>
  );
};

export default ClientWorks;

import styled from "styled-components";

const Wrapper = styled.div``;

const ClientProfile = () => {
  return (
    <Wrapper>
      <h1>Profile</h1>
    </Wrapper>
  );
};

export default ClientProfile;
